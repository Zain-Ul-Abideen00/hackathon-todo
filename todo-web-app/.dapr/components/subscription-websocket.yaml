# Subscription for websocket-service to receive task-updates for real-time broadcasts
apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: websocket-task-updates
spec:
  topic: task-updates
  routes:
    default: /api/updates/handle
  pubsubname: taskpubsub
scopes:
  - websocket-service
