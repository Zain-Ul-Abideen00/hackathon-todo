# Subscription for audit-service to receive all task-events for logging
apiVersion: dapr.io/v2alpha1
kind: Subscription
metadata:
  name: audit-task-events
spec:
  topic: task-events
  routes:
    default: /api/audit/handle
  pubsubname: taskpubsub
scopes:
  - audit-service
