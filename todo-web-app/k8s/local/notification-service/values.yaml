# Notification Service Helm Chart Values

replicaCount: 1

nameOverride: ""
fullnameOverride: ""

image:
  repository: todo-notification-service
  tag: "latest"
  pullPolicy: Never  # Use Never for Minikube local images

service:
  type: ClusterIP
  port: 8001
  targetPort: 8001

resources:
  requests:
    memory: "128Mi"
    cpu: "50m"
  limits:
    memory: "256Mi"
    cpu: "200m"

probes:
  liveness:
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 10
    failureThreshold: 3
  readiness:
    path: /health
    initialDelaySeconds: 5
    periodSeconds: 5
    failureThreshold: 3

# Dapr sidecar configuration
dapr:
  enabled: true  # Required for pub/sub subscription
  appId: "notification-service"
  appPort: 8001
