# Dapr Secrets Store Component

# Configuration for Kubernetes secrets store via Dapr
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: kubernetes-secrets
  namespace: todo-app
spec:
  type: secretstores.kubernetes
  version: v1
  metadata: []
scopes:
  - backend
  - notification-service
